// Use the 3caPitalization exercise as a basis for this exercise.
// Modify the code, so that only the first letter is upper case, and the rest is lower case.
//Make sure that it can take names of any length!

// const name = "peter susanne mogens";
// const nameArr = name.split(" ");
// console.log(nameArr);
// nameArr.forEach((element) =>
//   console.log(`${element[0].toUpperCase()}${element.substring(1)}`),
// );

// Opfølgning med Klaus
// const name = "franKenHeimer";
// let threecapitalizeStr =
//   name.substring(0, 2).toLowerCase() +
//   name.substring(2, 3).toUpperCase() +
//   name.substring(3).toLowerCase();

// string method opgave
